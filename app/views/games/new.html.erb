<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="new">
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, :maxlength => 10 %>
  </div>
 <div class="field">
   <%= f.label :player2_id %>
   <%= f.collection_select :player2_id, User.all.reject {|user| user == current_user}, :id, :username %>
 </div> 

  <div class="actions">
    <%= f.button %>
  </div>
<% end %>

<%= link_to 'Back', games_path %>
</div>