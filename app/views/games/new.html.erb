<%= form_for(@game) do |f| %>
<% if @game.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

  <ul>
    <% @game.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class='main2'>
  <div class='whitebackground'>
    <div class="new">
      <div class="field">
        <%= f.label :name %>
        <%= f.text_field :name, :maxlength => 10 %>
      </div>
      <%= f.submit :class => 'newgamebutton'%>
      <% end %>

      <a class='newgamebutton' href=<%= games_path %>>Back</a><br>
    </div>
  </div>
</div>